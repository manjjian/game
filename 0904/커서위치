커서 위치 제어
커서의 위치 이동 : 함수 gotoxy이용

구분: 커서의 위치제어 함수
visual c++ :없음
turbo c/c++: #include <conio.h> gotoxy(int x, int y);


void gotoxy (int x, int y)
{
  COORD Pos = (x -1, y -1);
  SetConsoleCurorPosition(GetStdHandle(STD_OUTPUT_HANDLE), Pos);
}
